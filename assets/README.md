# Assets

| File | Role |
|---|---|
| `jason-colapietro-github-avatar.png` | The rendered avatar used by the profile README, 800×800, displayed at 132 px. |
| `jason-colapietro-github-avatar-source.png` | The uncropped original, 1067×1475. Kept deliberately as the source the square render is cropped from. It is referenced by nothing and should not be deleted as unused. |

The rendered avatar is larger than it needs to be for a 132 px display and is a
candidate for re-encoding to a ~264 px WebP. Re-crop from the source file rather
than upscaling the render.
